{
  "name": "sh",
  "version": "1.0.0",
  "description": "A JS soundboard module.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild browser.js --bundle --outfile=sh.min.js --minify --platform=browser",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NonagonNetwork/sh.git"
  },
  "author": "NonagonNetwork",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/NonagonNetwork/sh/issues"
  },
  "homepage": "https://github.com/NonagonNetwork/sh#readme",
  "dependencies": {
    "axios": "^0.26.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.25"
  }
}
